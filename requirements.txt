# ACAT – Python dependencies
# Install with:  pip install -r requirements.txt

customtkinter>=5.2.0
darkdetect>=0.8.0
praat-parselmouth>=0.4.3
praatio>=6.0.0
pandas>=2.0.0
numpy>=1.24.0
pydub>=0.25.1
pyinstaller>=6.0.0        # only needed for building the distributable
